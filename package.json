{
	"name": "mern-task-manager",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"start": "npm run frontendStart && npm run backendStart",
		"frontendStart": "cd frontend  && npm run build",
		"backendStart": "cd backend  && npm start",
		"dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" "
	},
	"devDependencies": {
		"concurrently": "^8.0.1"
	}
}
